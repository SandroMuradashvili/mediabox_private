<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- 1. The Player: Using texture_view for perfect Z-index and no gray flashes -->
    <androidx.media3.ui.PlayerView
        android:id="@+id/playerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:use_controller="false"
        app:surface_type="texture_view"
        app:resize_mode="fixed_width"
        app:shutter_background_color="@android:color/transparent"
        app:show_buffering="never"
        app:use_artwork="false" />

    <!-- 2. The Placeholder: Matches main background, hides decoder initialization -->
    <View
        android:id="@+id/videoPlaceholder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#0D1117"
        android:visibility="visible" />

    <!-- 3. Control Overlay -->
    <include
        android:id="@+id/controlOverlay"
        layout="@layout/overlay_controls"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- 4. EPG Overlay -->
    <include
        android:id="@+id/epgOverlay"
        layout="@layout/overlay_epg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- 5. Branded Loading Indicator -->
    <ProgressBar
        android:id="@+id/loadingIndicator"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="center"
        android:indeterminateTint="@color/primary"
        android:visibility="gone" />

</FrameLayout>