<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Brighter base — was #B3080F18, now a lighter navy -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#CC1A2540"/>
            <corners android:radius="16dp"/>
        </shape>
    </item>
    <!-- Indigo border glow, slightly stronger -->
    <item>
        <shape android:shape="rectangle">
            <stroke android:width="1dp" android:color="#506366F1"/>
            <corners android:radius="16dp"/>
        </shape>
    </item>
</layer-list>